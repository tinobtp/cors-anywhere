#!/bin/bash
# Install nodejs:
# curl -sL install-node.vercel.app/lts | bash

# Use `npm install` to make sure that all dependencies (and the correct
# version) in package.json are installed.
node install
